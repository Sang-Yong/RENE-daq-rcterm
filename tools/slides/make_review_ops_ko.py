# -*- coding: utf-8 -*-
"""종합 리뷰 — 운영 측면 (한글).

코드를 모르는 사람도 읽을 수 있게 쓴다. 용어를 쓰면 그 자리에서 풀어 준다.
글머리 기호만 늘어놓지 않고 그림으로 보여 준다.
"""
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from deck import *
from pptx.enum.text import PP_ALIGN

d = Deck()

# ============================================================ 표지
d.title("RENE 실험",
        "데이터는 어디로 가고, 무엇이 지켜 주는가",
        "중성미자를 세는 실험이 하루에 300 기가바이트씩 만들어 냅니다.\n"
        "그 데이터가 사라지지 않도록 무엇을 해 두었는지, 코드를 모르는 사람도 읽을 수 있게 정리했습니다.",
        ["영광 사이트  ·  2026년 9월",
         "용어가 나오면 그 자리에서 풀어 씁니다"])

# ============================================================ 무엇을 하나
s = d.head("먼저", "우리가 하는 일을 한 문장으로")
d.box(s, M, 1.95, CW, 1.05, fill=PANEL)
d.text(s, M + 0.35, 2.20, CW - 0.7, 0.6,
       [[("원자로에서 나오는 ", {"size": 19, "color": INK}),
         ("중성미자", {"size": 19, "bold": True, "color": ACCENT}),
         ("를 세기 위해, 검출기가 내는 신호를 ", {"size": 19, "color": INK}),
         ("빠짐없이 기록하고 안전하게 보관", {"size": 19, "bold": True, "color": ACCENT}),
         ("합니다.", {"size": 19, "color": INK})]])
d.text(s, M, 3.25, CW, 0.3,
       [[("검출기에는 빛을 재는 장치가 두 종류 있습니다. 이름은 FADC 와 SADC 인데, "
          "카메라 두 대라고 생각하면 됩니다.", {"size": 15, "color": INK})]])
d.node(s, M, 3.75, 5.6, 1.05, "FADC", "빛의 '모양'을 통째로 찍는다 (파형)", "info", tsize=16, ssize=12)
d.node(s, 7.0, 3.75, CW - 6.25, 1.05, "SADC", "빛의 '양'만 숫자로 적는다", "info", tsize=16, ssize=12)
d.note(s, M, 5.10, CW, 1.55, "두 대가 같은 순간을 찍습니다 — 그래서 나중에 짝을 맞춰야 합니다",
       "카메라 두 대로 같은 장면을 찍으면, 나중에 '이 사진과 저 사진이 같은 순간' 이라고 "
       "이어 붙여야 쓸 수 있습니다. 그 짝 맞추는 일을 컴퓨터가 자동으로 합니다. "
       "이것을 후처리라고 부릅니다.", "info")

# ============================================================ 데이터 흐름
s = d.head("데이터가 지나가는 길", "만들어져서 → 정리되고 → 세 군데에 나뉘어 보관됩니다")
xs, bw = d.flow(s, 2.00, [("검출기", "신호가 나온다", "dark"),
                          ("빠른 디스크", "여기에 먼저 쌓인다", "ok"),
                          ("큰 디스크", "정리를 마치면 옮긴다", "info"),
                          ("보관 창고", "140 테라바이트", "ok")], h=1.00,
                labels=["1분마다\n파일 하나", "정리 끝나면", "대조 뒤"])
d.text(s, M, 3.20, CW, 0.3,
       [[("옮길 때마다 '제대로 갔는지' 를 한 파일씩 대조하고, 통과한 것만 원본을 지웁니다. "
          "먼저 지우고 나중에 확인하는 일은 없습니다.", {"size": 15, "color": INK})]])
d.text(s, M, 3.75, CW, 0.3, [[("그리고 여기서 두 갈래로 더 나갑니다 —", {"size": 15, "bold": True, "color": INK})]])
d.node(s, M, 4.20, 5.6, 1.15, "경희대 서버", "인터넷으로 보내는 사본\n불이 나도 남는다", "ok", tsize=15, ssize=12)
d.node(s, 7.0, 4.20, CW - 6.25, 1.15, "외장하드", "선반에 꽂아 두는 사본\n인터넷과 무관하다", "ok", tsize=15, ssize=12)
d.note(s, M, 5.60, CW, 1.20, "왜 굳이 세 벌인가",
       "디스크는 고장 납니다. 건물에 사고가 날 수도 있습니다. 사본이 한 벌뿐이면 "
       "그 한 번으로 실험 몇 달치가 사라집니다.", "warn")

# ============================================================ 왜 옮기나
s = d.head("왜 자꾸 옮기나", "옮기지 않으면 실험이 멈추기 때문입니다")
d.text(s, M, 1.95, CW, 0.3,
       [[("검출기 바로 옆의 빠른 디스크는 3.7 테라바이트뿐입니다. 하루에 약 0.3 테라바이트가 쌓이니 —",
          {"size": 15.5, "color": INK})]])
for i, (lab, frac, col, note) in enumerate([
        ("가만히 두면  12일 뒤", 1.00, CRIT, "가득 참 → 기록할 곳이 없어 수집이 멈춘다"),
        ("옮기면서 쓰면", 0.18, OK, "언제나 여유가 남는다 (지금 3.1 TB 비어 있음)")]):
    yy = 2.55 + i * 1.15
    d.text(s, M, yy, 3.4, 0.3, [[(lab, {"size": 15, "bold": True, "color": INK})]])
    d.bar(s, M + 3.5, yy - 0.02, 5.2, 0.36, frac, color=col)
    d.text(s, M + 9.0, yy, CW - 8.25, 0.3, [[(note, {"size": 13.5, "color": INK2})]])
d.note(s, M, 4.95, CW, 1.20, "이 일이 멈추면 무슨 일이 나나",
       "디스크가 차는 순간 검출기는 계속 신호를 내는데 적을 곳이 없어집니다. "
       "그 시간 동안의 데이터는 영영 없습니다. 그래서 이 옮기는 일을 사람이 잊지 않도록 "
       "컴퓨터가 알아서 하고, 막히면 소리를 내고 메일을 보냅니다.", "crit")
d.text(s, M, 6.35, CW, 0.3,
       [[("※ '테라바이트' 는 기가바이트의 1,024배입니다. 3.7 테라바이트면 영화 약 900편 분량입니다.",
          {"size": 12.5, "color": MUTED})]])

# ============================================================ 후처리
s = d.head("후처리", "카메라 두 대의 사진을 짝짓고, 쓸 수 있는 형태로 바꿉니다")
xs, bw = d.flow(s, 2.05, [("FADC 파일", "카메라 A", "info"),
                          ("SADC 파일", "카메라 B", "info"),
                          ("합친 파일", "짝을 맞춘 것", "warn"),
                          ("분석용 파일", "물리량으로 바꾼 것", "ok")], h=0.95, wtot=9.6,
                labels=["", "짝짓기", "변환"])
d.text(s, M, 3.25, CW, 0.3,
       [[("여기서 중요한 규칙이 하나 있습니다 — ", {"size": 15, "color": INK}),
         ("짝짓기는 순서를 지켜야 합니다.", {"size": 15, "bold": True, "color": CRIT})]])
d.box(s, M, 3.70, CW, 1.15, fill=PANEL)
d.text(s, M + 0.35, 3.92, CW - 0.7, 0.75,
       [[("1분짜리 파일을 처리하고 나면 '다음 파일은 여기서부터' 라는 표시를 남깁니다. "
          "그 표시를 다음 파일이 이어받아야 합니다. 건너뛰거나 동시에 처리하면 "
          "그 경계에서 데이터를 잃습니다.", {"size": 15, "color": INK})]])
d.note(s, M, 5.05, 6.3, 1.55, "그래서 순서대로 한 개씩",
       "짝짓기만 그렇고, 그 뒤의 '변환' 은 파일끼리 상관이 없어 여러 개를 동시에 합니다. "
       "지금은 세 개씩 돌립니다.", "ok")
d.note(s, 7.3, 5.05, CW - 6.55, 1.55, "수집보다 3~4개 뒤에서 따라갑니다",
       "지금 기록 중인 파일은 아직 다 안 쓰였기 때문에 일부러 조금 뒤에서 처리합니다. "
       "밀린 것이 아니라 그래야 안전합니다.", "info")

# ============================================================ 10G
s = d.head("길을 넓혔습니다", "보관 창고로 가는 길이 65배 빨라졌습니다")
d.text(s, M, 1.95, CW, 0.3,
       [[("보관 창고는 다른 컴퓨터에 있습니다. 거기까지 랜선으로 데이터를 보내는데, "
          "그 선이 생각보다 훨씬 느렸습니다.", {"size": 15, "color": INK})]])
for i, (lab, frac, col, val) in enumerate([
        ("전  (좁은 길)", 0.014, MUTED, "초당 7.7 메가바이트"),
        ("후  (넓은 길)", 1.00, OK, "초당 534 메가바이트")]):
    yy = 2.55 + i * 0.95
    d.text(s, M, yy, 2.9, 0.3, [[(lab, {"size": 15, "bold": True, "color": INK})]])
    d.bar(s, M + 3.0, yy - 0.02, 6.0, 0.38, frac, color=col)
    d.text(s, M + 9.3, yy, CW - 8.55, 0.3, [[(val, {"font": MONO, "size": 14, "color": col})]])
d.text(s, M, 4.35, CW, 0.3,
       [[("하루치 데이터를 창고로 옮기는 데 ", {"size": 15, "color": INK}),
         ("12시간이 걸리던 것이 11분", {"size": 15, "bold": True, "color": OK}),
         ("이 됐습니다.", {"size": 15, "color": INK})]])
d.note(s, M, 4.85, CW, 1.80, "고치는 것보다 원인을 찾는 게 어려웠습니다",
       "새 랜카드를 꽂았는데도 통신이 안 됐습니다. 알고 보니 예전에 쓰던 랜카드가 "
       "케이블이 빠진 채로도 목록에 남아 있었고, 컴퓨터가 그 죽은 길로 데이터를 계속 "
       "내보내고 있었습니다. 옛 것을 정리하고 나서야 새 길이 열렸습니다.\n"
       "— 새로 만든 것이 안 될 때, 옛 것이 남아 가로채고 있지는 않은지 봐야 한다는 교훈입니다.", "warn")

# ============================================================ 외장하드
s = d.head("외장하드 백업", "하드가 차면 다음 하드로 저절로 이어집니다")
d.text(s, M, 1.92, CW, 0.3,
       [[("보관 창고도 언젠가는 찹니다. 그래서 오래된 데이터를 외장하드에 옮겨 담고 "
          "창고를 비웁니다. 예전에는 하드 한 장이 차면 거기서 멈췄습니다.",
          {"size": 15, "color": INK})]])
# 그림 : 하드 두 장과 인계
d.node(s, M, 2.55, 3.5, 1.25, "하드 1", "가득 참 (1.7 TB)", "ok", tsize=16, ssize=12)
d.arrow(s, M + 3.62, 3.15, 1.0, "이어서")
d.node(s, M + 4.75, 2.55, 3.5, 1.25, "하드 2", "채우는 중", "info", tsize=16, ssize=12)
d.arrow(s, M + 8.37, 3.15, 1.0, "다 차면")
d.node(s, M + 9.5, 2.55, CW - 9.5, 1.25, "멈춤", "교체 안내 메일", "warn", tsize=16, ssize=12)
d.text(s, M, 4.00, CW, 0.3,
       [[("하드가 찰 때마다 메일이 옵니다 — ", {"size": 15, "color": INK}),
         ("\"이 하드는 이제 뽑아 보관하셔도 됩니다\"", {"size": 15, "bold": True, "color": OK}),
         (". 하드의 모델·일련번호·담긴 내용이 메일에 적혀 있습니다.", {"size": 15, "color": INK})]])
d.note(s, M, 4.50, 6.3, 1.35, "다음 하드가 없으면 그냥 멈춥니다",
       "다음 하드가 안 꽂혀 있거나 이미 가득 차 있으면, 억지로 진행하지 않고 "
       "이유를 적고 멈춘 뒤 알립니다.", "info")
d.note(s, 7.3, 4.50, CW - 6.55, 1.35, "하드보다 큰 데이터도 나눠 담습니다",
       "6.7 테라바이트짜리 덩어리 하나가 1.8 테라바이트 하드 두 장에 나뉘어 "
       "온전히 담겼습니다. 어느 조각이 어느 하드에 있는지 목록에 남습니다.", "ok")
d.text(s, M, 6.05, CW, 0.3,
       [[("※ 그 서버는 인터넷에 연결돼 있지 않아 스스로 메일을 못 보냅니다. "
          "그래서 메일을 '파일' 로 남겨 두면 옆 컴퓨터가 5분마다 가져가 대신 보냅니다.",
          {"size": 12.5, "color": MUTED})]])

# ============================================================ 공간 확보
s = d.head("공간을 크게 비웠습니다", "안 써도 되는 중간 파일을 지웠습니다")
d.text(s, M, 1.95, CW, 0.3,
       [[("사진으로 치면 이렇습니다 — ", {"size": 15, "color": INK}),
         ("원본 사진", {"size": 15, "bold": True, "color": ACCENT}),
         ("이 있고, 그것을 ", {"size": 15, "color": INK}),
         ("편집 중간 파일", {"size": 15, "bold": True, "color": WARN}),
         ("을 거쳐 ", {"size": 15, "color": INK}),
         ("완성본", {"size": 15, "bold": True, "color": OK}),
         ("으로 만듭니다.", {"size": 15, "color": INK})]])
d.flow(s, 2.45, [("원본", "지운 적 없음", "info"),
                 ("중간 파일", "★ 이것을 지웠다", "warn"),
                 ("완성본", "분석에 쓰는 것", "ok")], h=0.95, wtot=8.6)
d.note(s, 9.9, 2.45, CW - 9.15, 0.95, "중간 파일은 원본에서 다시 만들 수 있습니다", "", "info")
d.text(s, M, 3.75, CW, 0.3,
       [[("지우기 전에 ", {"size": 15, "color": INK}),
         ("정말 버려도 되는지 하나하나 맞대어 확인", {"size": 15, "bold": True, "color": CRIT}),
         ("했습니다. 완성본이 중간 파일의 내용을 전부 갖고 있었습니다.", {"size": 15, "color": INK})]])
for i, (lab, frac, col, val) in enumerate([
        ("청소 전", 0.14, WARN, "19.4 테라바이트 남음"),
        ("청소 후", 0.41, OK, "58 테라바이트 남음")]):
    yy = 4.25 + i * 0.72
    d.text(s, M, yy, 2.2, 0.3, [[(lab, {"size": 15, "bold": True, "color": INK})]])
    d.bar(s, M + 2.3, yy - 0.02, 6.4, 0.34, frac, color=col)
    d.text(s, M + 9.0, yy, CW - 8.25, 0.3, [[(val, {"font": MONO, "size": 13.5, "color": col})]])
d.note(s, M, 5.75, CW, 1.05, "런 1,394개에서 34.8 테라바이트를 되찾았습니다",
       "한 번에 다 지우지 않고 한 번에 조금씩만 지웁니다 — 그 일에 매달려 다른 일이 밀리지 않게.", "ok")

# ============================================================ 문제가 생기면
s = d.head("문제가 생기면", "사람이 지켜보지 않아도 알 수 있게 해 두었습니다")
xs, bw = d.flow(s, 2.00, [("이상 발생", "", "crit"),
                          ("스스로 고쳐 본다", "보드 재설정 등", "warn"),
                          ("소리로 알린다", "현장 경보음", "warn"),
                          ("메일을 보낸다", "담당자 · 전문가", "info")], h=0.95,
                labels=["감지", "실패하면", "동시에"])
d.text(s, M, 3.20, CW, 0.3,
       [[("경보음은 ", {"size": 15, "color": INK}),
         ("사람이 끌 때까지", {"size": 15, "bold": True, "color": CRIT}),
         (" 계속 울립니다. 끄는 것과 고치는 것은 다릅니다.", {"size": 15, "color": INK})]])
y = d.table(s, M, 3.48, CW,
            ["무엇이 이상한가", "어떻게 아나", "무엇이 자동으로 되나"],
            [["수집이 멈췄다", "상태 기록이 갱신되지 않는다", "다시 띄운다"],
             ["신호가 안 들어온다", "계수율이 뚝 떨어진다", "메일 — 사람이 가야 한다"],
             ["정리 작업이 죽었다", "5분마다 확인한다", "메일"],
             ["보관 창고가 빠졌다", "마운트를 확인한다", "옮기기를 멈추고 기다린다"],
             ["하드가 가득 찼다", "백업이 스스로 판단", "메일 — 하드를 갈면 이어진다"]],
            widths=[3.5, 4.3, CW - 7.8], rh=0.38, size=13)
d.note(s, M, 5.88, CW, 1.30, "알림을 너무 많이 보내면 사람이 무시하게 됩니다",
       "그래서 같은 일로 반복해 보내지 않고, 정비하느라 일부러 멈춰 둔 동안에는 울리지 않습니다.", "info")

# ============================================================ 실제 사고
s = d.head("실제로 있었던 일", "세 가지와, 그래서 무엇을 바꿨는가")
for i, (when, what, why, fix) in enumerate([
        ("9월 1일", "신호가 0 이 됐다",
         "검출기의 고전압이 꺼져 있었다", "장비를 30분 뒤진 뒤에야 알았다 → 이제는 고전압부터 확인한다"),
        ("9월 4일", "백업이 7시간 반 만에 끊겼다",
         "두 컴퓨터가 같은 데이터를 만졌다 — 한쪽이 옮기는 파일을 다른 쪽이 지웠다",
         "지우는 쪽과 담는 쪽이 겹치지 않도록 아예 나눴다"),
        ("9월 4일", "하드가 덜 찬 채로 멈췄다",
         "이미 담아 둔 파일을 '새로 담았다' 고 세고 있었다",
         "이미 있는 것은 세지 않도록 고쳤다. 이제 끝까지 채운다")]):
    yy = 1.95 + i * 1.55
    d.box(s, M, yy, CW, 1.40, fill=PANEL)
    d.box(s, M, yy, 0.05, 1.40, fill=CRIT if i else WARN)
    d.text(s, M + 0.28, yy + 0.16, 1.5, 0.28, [[(when, {"font": MONO, "size": 12, "bold": True, "color": MUTED})]])
    d.text(s, M + 1.85, yy + 0.14, 4.3, 0.3, [[(what, {"size": 15.5, "bold": True, "color": INK})]])
    d.text(s, M + 1.85, yy + 0.52, CW - 2.2, 0.3, [[("왜 : " + why, {"size": 13.5, "color": INK2})]])
    d.text(s, M + 1.85, yy + 0.92, CW - 2.2, 0.3, [[("→ " + fix, {"size": 13.5, "color": OK})]])
d.text(s, M, 6.70, CW, 0.3,
       [[("세 가지 모두 '누가 실수했나' 가 아니라 '어떻게 하면 다음에 안 그럴까' 로 정리해 기록에 남겼습니다.",
          {"size": 13.5, "color": MUTED})]])

# ============================================================ 지금
s = d.head("지금 상태", "숫자로")
nums = [("수집", "run 4332", "쉬지 않고 돌고 있음", "ok"),
        ("계수율", "약 1,000 Hz", "정상 범위", "ok"),
        ("빠른 디스크", "3.1 TB 여유", "넉넉함", "ok"),
        ("보관 창고", "58 TB 여유", "청소 전 19.4 TB", "ok"),
        ("외장하드", "2장 운용", "차면 자동 인계", "ok"),
        ("자동 감시", "5분마다", "사람 없이도 본다", "ok")]
for i, (t, v, sub, kind) in enumerate(nums):
    x = M + (i % 3) * (CW / 3.0)
    yy = 2.10 + (i // 3) * 1.55
    d.box(s, x, yy, CW / 3.0 - 0.25, 1.30, fill=PANEL)
    d.text(s, x + 0.28, yy + 0.16, CW / 3.0 - 0.8, 0.28,
           [[(t, {"font": MONO, "size": 11.5, "color": MUTED})]])
    d.text(s, x + 0.28, yy + 0.48, CW / 3.0 - 0.8, 0.40,
           [[(v, {"size": 21, "bold": True, "color": OK})]])
    d.text(s, x + 0.28, yy + 0.95, CW / 3.0 - 0.8, 0.28,
           [[(sub, {"size": 12.5, "color": INK2})]])
d.note(s, M, 5.30, CW, 1.35, "사람이 매일 해야 할 일은 사실상 없습니다",
       "화면을 한 번 보고 세 가지(수집이 도는가 · 계수율이 정상인가 · 경보가 떴는가)만 "
       "확인하면 됩니다. 나머지는 알아서 돌고, 막히면 먼저 알려 옵니다.", "ok")

# ============================================================ 사람 몫
s = d.head("사람이 해야 하는 것", "기계가 대신할 수 없는 것들")
d.bullets(s, M, 2.05, CW, [
    ("하드를 갈아 끼우기 — ", "가득 찼다는 메일이 오면 뽑아서 보관하고 새것을 꽂습니다"),
    ("하드에 이름표 붙이기 — ", "일련번호를 함께 적으세요. 컴퓨터가 붙이는 이름은 꽂을 때마다 바뀝니다"),
    ("검출기 쪽 확인 — ", "신호가 0 이면 장비보다 고전압을 먼저 의심하세요"),
    ("보드가 먹통일 때 전원 재투입 — ", "원격으로 못 합니다. 그리고 전원을 끊으면 설정이 날아가므로 반드시 다시 넣어야 합니다"),
    ("기록 남기기 — ", "어느 하드를 어디에 두었는지는 사람만 압니다"),
], size=15, gap=13)
d.note(s, M, 5.35, CW, 1.30, "그리고 하나 — 같은 데이터를 두 곳에서 동시에 만지지 않기",
       "9월 4일 사고가 그것이었습니다. 무언가를 시작하기 전에 '지금 돌고 있는 것이 있나' 를 "
       "한 번 보는 것만으로 막을 수 있습니다.", "warn")

d.save(os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "..", "docs", "review_ops_ko.pptx"))
print("docs/review_ops_ko.pptx")
