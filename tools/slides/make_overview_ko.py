# -*- coding: utf-8 -*-
"""종합 발표자료 (한글) — 구조 / 개선한 것 / 실측 / 앞으로."""
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from deck import *
from pptx.enum.text import PP_ALIGN

d = Deck()

# ============================================================ 표지
d.title("RENE / CUPDAQ",
        "텍스트 런컨트롤과\n무인 운용 체계",
        "GUI 런컨트롤을 다시 쓴 데서 출발해, 수집·후처리·백업·감시·알람까지\n"
        "사람이 지켜보지 않아도 도는 하나의 사슬로 묶었다.",
        ["영광 사이트  ·  Rocky Linux 9.8  ·  ROOT 6.28/04  ·  GCC 11.5.0",
         "2026년 8월  ·  run 4280~4303  ·  24시간 런 5회 완주 · 무인 연속 운용"])

# ============================================================ 한눈에
s = d.head("한눈에", "지금 어디까지 와 있나")
cards = [("수집", "24시간 로테이션", "5회 완주", "ok"),
         ("후처리", "수집을 실시간 추적", "--lag 3", "ok"),
         ("데이터 이동", "로컬 → 백업 → 보관", "체크섬 대조", "ok"),
         ("외부 백업", "경희대 서버로 성격별", "내용 대조", "ok"),
         ("모니터링", "livetime → IBD 후보", "3단계 자동", "ok"),
         ("알람·메일", "소리 + 전문가 메일", "발송 확인", "ok"),
         ("문제 런 관리", "격리하고 한 목록으로", "전 구간 훑음", "ok"),
         ("자동 복구", "USB 보드 되살리기", "실전 대기", "warn")]
bw, gap = (CW - 0.30 * 3) / 4.0, 0.30
for i, (t, sub_, chip, kind) in enumerate(cards):
    x = M + (i % 4) * (bw + gap)
    y = 2.15 + (i // 4) * 1.42
    d.box(s, x, y, bw, 1.22, fill=PANEL)
    d.box(s, x, y, 0.05, 1.22, fill=ACCENT)
    d.text(s, x + 0.22, y + 0.16, bw - 0.42, 0.3,
           [[(t, {"size": 16, "bold": True, "color": INK})]])
    d.text(s, x + 0.22, y + 0.52, bw - 0.42, 0.3,
           [[(sub_, {"size": 12.5, "color": INK2})]])
    d.chip(s, x + 0.22, y + 0.84, 1.75, 0.28, chip, kind, size=10.5)
d.note(s, M, 5.20, CW, 1.05, "한 문장으로",
       "화면 앞에 사람이 앉아 있어야 돌던 것을, 사람이 없어도 돌고 "
       "문제가 생기면 사람을 부르는 것으로 바꿨다.", "info")
d.foot(s, "2026-08-21 기준  ·  run 4303 수집 중")

# ============================================================ 진행 경과
s = d.head("진행 경과", "8일 동안 무엇이 있었나")
tl = [("08-13", "빌드와 로직을 먼저 검증했다", "실제 ROOT clean build · 감시자 로테이션/복구 시험 통과"),
      ("08-14", "영광 사이트에서 처음 돌렸다", "run 4280~4287. 무인 운용에서만 드러나는 결함 3종을 만났다"),
      ("08-15", "그 결함 3종을 고쳤다", "로테이션이 더 이상 기록을 잃지 않는다. DAQ 를 tmux 로 옮겼다"),
      ("08-16", "수정본을 두 번 연속 관찰했다", "4288 → 4289 → 4290 무결. 후처리가 수집을 실시간으로 따라잡았다"),
      ("08-17", "데이터가 흐르는 길을 다시 설계했다", "NFS 가 100 Mb 링크임을 실측 · 3단계 이동과 외부 백업을 붙였다"),
      ("08-18", "모니터링 입력을 PRD 로 일원화했다", "세 단계 모두 분석 체인과 수치가 일치함을 대조로 확인했다"),
      ("08-19", "이동을 체크섬 대조 뒤 삭제로 바꿨다", "외부 접속 6.5개월치를 전수 조사하고 DAQ 포트를 닫았다"),
      ("08-20", "FADC 보드가 걸려 2시간 9분 멎었다", "복구 절차를 세우고 알람 · 메일 · 자동 USB 복구를 붙였다"),
      ("08-21", "죽은 런을 격리하는 체계를 만들었다", "전 구간 1,972 런을 훑고, 막혀 있던 런들을 풀었다")]
lx = M + 1.02
d.box(s, lx, 2.22, 0.028, len(tl) * 0.46 - 0.10, fill=FAINT)
for i, (dt, what, res) in enumerate(tl):
    y = 2.15 + i * 0.46
    d.text(s, M, y + 0.05, 0.88, 0.26,
           [[(dt, {"font": MONO, "size": 13, "bold": True, "color": ACCENT})]],
           align=PP_ALIGN.RIGHT)
    d.box(s, lx - 0.055, y + 0.10, 0.14, 0.14, fill=ACCENT,
          shape=MSO_SHAPE.OVAL)
    d.text(s, lx + 0.28, y + 0.02, 4.15, 0.28,
           [[(what, {"size": 14, "bold": True, "color": INK})]])
    d.text(s, lx + 4.55, y + 0.04, CW - (lx - M) - 4.62, 0.28,
           [[(res, {"size": 12.5, "color": INK2})]])
d.foot(s, "왼쪽이 한 일, 오른쪽이 그 결과다. 세 줄이 장애 대응이고 나머지는 그 장애가 다시 오지 않게 만든 것이다")

# ============================================================ 만든 것
s = d.head("만든 것", "8일치 산출물")
rows = [[("rcterm · rcsupervisor", {"bold": True}), ("C++17  2,563 줄", {"font": MONO}),
         "런 1회 / N시간 로테이션 + 진단 + 자동 복구"],
        [("운용 스크립트", {"bold": True}), ("15 개", {"font": MONO}),
         "후처리 · 이동 · 백업 · 알람 · 복구 · 문제 런 · 화면"],
        [("도구", {"bold": True}), ("20 개", {"font": MONO}),
         "모니터링 3단계 · 메일 · 구글시트 · 이 발표자료"],
        [("문서", {"bold": True}), ("5 편 + 스킬 1", {"font": MONO}),
         "운용 · 후처리 · 데이터 흐름 · 알람 · 문제 런 목록"],
        [("커밋", {"bold": True}), ("77 개", {"font": MONO}),
         "08-13 부터. 전부 origin/main 에 있다"]]
d.table(s, M, 2.15, CW, ["무엇", "규모", "하는 일"], rows,
        widths=[3.1, 2.3, CW - 5.4], rh=0.44)
d.note(s, M, 5.05, CW, 1.02, "복제하지 않는 것이 원칙이다",
       "하드웨어 드라이버는 한 줄도 쓰지 않았다 — 제어는 CUPDAQ 바이너리가 하고 우리는 "
       "32바이트 명령만 보낸다. 물리도 같다 — 후처리 매크로는 호출하고, 분석 컷 상수는 "
       "그 헤더를 그대로 include 한다. 베끼면 두 벌이 갈라지기 때문이다.", "info")
d.note(s, M, 6.22, CW, 0.72, "운용 실적",
       "24시간 런 5회 완주 (4288 · 4289 · 4292 · 4294 · 4302) — 합계 약 4억 3,396만 이벤트.", "ok")

# ============================================================ 01 구조
d.section("01", "구조", "무엇이 무엇을 제어하고, 왜 그렇게 나눴는가")

s = d.head("구조", "하드웨어를 건드리는 것은 우리가 아니다")
d.text(s, M, 2.10, CW, 0.5,
       [[("이 프로젝트를 고칠 때 보드 동작을 걱정할 필요가 없다. 반대로 보드가 "
          "이상하면 이쪽이 아니라 CUPDAQ 을 봐야 한다.", {"size": 16, "color": INK2})]])
d.code(s, M, 2.60, CW, 3.15, [
    "사람 / 감시자",
    ("   │", MUTED),
    "   ├─ rcsupervisor ──(자식으로 실행, N시간마다 새 run 번호로 재시작)",
    ("   │      │", MUTED),
    "   │      └─ rcterm ──┬─ executedaq.sh ──►  daq / merger / tcb"
    + "",
    ("   │                 │                    ↑ 여기가 하드웨어 (CUPDAQ)", ACC2),
    "   │                 └─ 소켓 32 byte ────►  같은 프로세스들에 명령·질의",
    ("   │", MUTED),
    "   ├─ postrun.sh    수집을 뒤따라가며 merge + production",
    "   ├─ dataflow.sh   끝난 런을 옮긴다 (로컬 → 백업대기 → 외부백업 → 보관)",
    "   ├─ tools/monitor 끝난 런에서 물리 지표를 뽑는다",
    "   └─ daq-notify    사건을 소리와 메일로 내보낸다",
], size=12)
d.note(s, M, 5.95, CW, 0.88, "핵심 전제",
       "런컨트롤이 하는 일은 셋뿐이다 — 노드 목록을 읽고, 노드마다 실행 스크립트를 "
       "돌리고, 32바이트 명령을 주고받는다. 하드웨어 드라이버는 한 줄도 다시 쓰지 않았다.",
       "info")

s = d.head("구조", "설계를 이렇게 정한 이유")
rows = [
    [("부팅 순서", {"font": MONO, "bold": True}), "이름 정렬이 아니라 노드 역할로 강제",
     "정렬에 맡기면 TCB 가 먼저 떠서 클라이언트 접속이 깨진다"],
    [("상태 판정", {"font": MONO, "bold": True}), "정수가 아니라 비트마스크",
     "정수로 비교하면 조용히 틀린다. 소스 실측으로 확정"],
    [("ADC 종류", {"font": MONO, "bold": True}), "첫 글자가 아니라 부분문자열",
     "MERGER 가 MADC 로 오분류된다. 판정 불가는 에러로 정지"],
    [("종료 코드", {"font": MONO, "bold": True}), "0 정상 / 1 설정오류 / 2 런실패",
     "감시자가 '재시작이 무의미' 와 '재시작할 가치 있음' 을 구분한다"],
    [("감시자 분리", {"font": MONO, "bold": True}), "상태머신을 링크하지 않는다",
     "상태머신 버그가 감시자까지 끌고 내려가면 자동 복구가 무너진다"],
    [("정지 신호", {"font": MONO, "bold": True}), "그룹이 아니라 rcterm 한 프로세스에만",
     "그룹에 보내면 DAQ 가 쓰기 도중 죽어 마지막 파일이 상한다"],
]
d.table(s, M, 2.15, CW, ["무엇을", "어떻게", "왜"], rows,
        widths=[2.2, 4.0, CW - 6.2], rh=0.62, size=13.5)
d.foot(s, "근거는 저장소 CLAUDE.md §3 (프로토콜) 과 §5 (설계 판단) 에 있다 — 되돌리기 전에 이유부터 읽을 것")

# ============================================================ 데이터 흐름
s = d.head("데이터", "흐르는 길을 정한 것은 취향이 아니라 랜카드다")
d.text(s, M, 2.08, CW, 0.4,
       [[("이 PC 에는 랜카드가 둘이고 속도가 10배 다르다. 그래서 백업과 장기보관을 "
          "다른 길로 보낸다 — 동시에 돌려도 서로를 굶기지 않는다.",
          {"size": 15.5, "color": INK2})]])
d.flow(s, 2.72, [("/Data_ssd", "NVMe 3.7T · 수집+후처리", "ok"),
                 ("/data", "RAID 32T · 백업 대기", "info"),
                 ("khu 서버", "외부 백업 (1 Gb)", "info"),
                 ("/scratch", "NFS 140T · 장기보관", "warn")],
       h=1.0, labels=["1", "2", "3"])
d.table(s, M, 4.15, CW, ["단계", "넘어가는 조건"],
        [["1  /Data_ssd → /data", "후처리 완료(PRD 수 = FADC 수) · 수집 중 아님 · 최근 keep_ssd 개는 남긴다"],
         ["2  /data → 경희대", "성격별 카테고리 rsync 후 체크섬 대조. 통과한 것만 완료 표시"],
         ["3  /data → /scratch", "2단계를 통과한 런만. 여기가 최종 보관"]],
        widths=[3.4, CW - 3.4], rh=0.42, size=13.5)
d.note(s, M, 5.85, CW, 0.95, "옮기기 전에 백업하라 — 6배가 갈린다",
       "산출물이 로컬에 있는 동안 보내면 13.9 MB/s, /scratch 로 보낸 뒤 보내면 2.2 MB/s. "
       "수집 → 후처리 → 백업 → 이동 순서는 성능 면에서도 옳다.", "warn")

s = d.head("데이터", "파일 성격대로 나눠 보낸다")
d.code(s, M, 2.15, CW, 2.35, [
    "RAW/<run>/{FADC,SADC}_*.root.*   ──►  RAW/<run>/",
    "RAW/<run>/PRD/PRD_*.root         ──►  PRD/<run>/",
    "RAW/<run>/PNG/*.png              ──►  PNG/<run>/",
    "LOG/{TCB,FADCDAQ,SADCDAQ}_*.log  ──►  DAQLOG/<종류>/*.log.gz",
    "CONFIG/<run>.config              ──►  config/<run>.config",
    "runcatalog.db                    ──►  db/runcatalog.<날짜>.db",
    "",
    ("Merged/                          ──►  보내지 않는다", WARN),
])
d.bullets(s, M, 4.70, CW, [
    ("Merged 를 빼는 것은 의도적이다. ", "원격 트리에 그 카테고리가 없고, 런당 114 GB 인데 RAW 로부터 언제든 다시 만들 수 있다."),
    ("이동은 언제나 복사 → 체크섬 대조 → 삭제. ", "개수만 맞추는 것으로는 끊긴 전송의 잔재를 성공으로 오인한다."),
    ("체크섬은 비싸지 않다. ", "PRD 15.4 GB 기준 전송 17분 40초 대 대조 1분 00초 — 양쪽이 각자 계산해 결과만 주고받는다."),
], size=15)

# ============================================================ 모니터링
s = d.head("모니터링", "수집이 정상인지 물리 지표로 확인한다")
d.flow(s, 2.15, [("1  livetime", "PRD 의 Event 트리", "info"),
                 ("2  IBD 후보", "파형 → 에너지 → 페어링", "info"),
                 ("3  보정 rate", "효율 보정 + 추이 그림", "ok")],
       h=0.95, labels=["", ""])
d.text(s, M, 3.35, CW, 0.4,
       [[("세 단계 모두 production 산출물(PRD) 하나만 읽는다. 단계마다 매크로 하나 + "
          "스크립트 하나이고, 이미 처리한 런은 건너뛰므로 몇 번을 돌려도 안전하다.",
          {"size": 15.5, "color": INK2})]])
d.table(s, M, 4.05, CW, ["", "여기", "분석 체인", ""],
        [["single 목록 (run 4237 한 서브런)", "5,739", "5,739", ("비트 단위 일치", {"color": OK, "bold": True})],
         ["n-Gd 후보 / 우발", "2,097 / 1,377", "2,097 / 1,377", ("일치", {"color": OK, "bold": True})],
         ["n-H 후보 / 우발", "601,739 / 551,422", "601,739 / 551,422", ("일치", {"color": OK, "bold": True})]],
        widths=[5.0, 2.6, 2.6, CW - 10.2], rh=0.42, size=13.5)
d.note(s, M, 5.75, CW, 1.0, "물리는 복제하지 않는다",
       "파형→광전자 변환도 컷 상수도 분석 쪽 헤더를 그대로 include 한다. 베껴 두면 "
       "저쪽이 컷을 바꿨을 때 이 표만 조용히 틀린 값이 된다.", "info")

# ============================================================ 알람
s = d.head("감시", "문제가 생기면 사람을 부른다")
d.code(s, M, 2.12, CW, 3.05, [
    "런 하나 실패",
    ("     │", MUTED),
    "     ├─ 감시자가 새 번호로 재시작              ──►  알림 : restart",
    ("     │", MUTED),
    ("(연속 실패가 5회에 닿으면)", ACC2),
    ("     ▼", MUTED),
    "  usb-recover.sh",
    "     ├─ [1] 안전 확인   수집이 살아 있으면 아무것도 안 한다",
    "     ├─ [2] 진단        USB 오류 근거가 없으면 손대지 않는다",
    "     ├─ [3] usbreset    최대 2회. 매번 3분 확인 런으로 검사",
    ("     │      ├─ 통과 → 실패 카운터를 되돌리고 수집 계속", OK),
    ("     │      └─ 실패 → 알람 + 전문가 메일", CRIT),
], size=12.5)
d.bullets(s, M, 5.42, CW, [
    ("알람은 두 갈래로 낸다. ", "사운드카드와 케이스 내부 비프 — 한쪽이 죽어도 침묵이 되지 않는다. 사람이 끌 때까지 반복한다."),
    ("메일 본문에 진단을 담는다. ", "수집이 도는가 · 보드 셋이 보이는가 · 어느 로그에 USB 오류가 있는가 · 감시자 로그 15줄."),
], size=15)

# ============================================================ 02 개선
d.section("02", "개선한 것", "무엇이 틀려 있었고, 어떻게 바로잡았는가")

s = d.head("개선 · 원본", "GUI 런컨트롤이 갖고 있던 결함")
d.table(s, M, 2.12, CW, ["", "무엇이", "결과"],
        [["1", "split time 을 정수로 넘김", "실행 즉시 TypeError"],
         ["2", "부팅 순서를 dopt 문자열로 정렬", "AMOREADC 가 있으면 TCB 가 마지막이 아니게 된다"],
         ["3", "ADC 종류를 이름 첫 글자로 판정", "MERGER 가 MADC 로 오분류"],
         ["4", "존재하지 않는 함수 호출", "기존 텍스트판은 실행 자체가 불가능했다"],
         ["5", "주석과 코드가 서로 반대", "분↔초 환산 방향이 뒤집혀 있었다"],
         ["6", "로그 디렉터리를 만들지 않음", "DAQ 가 아무 말 없이 죽는다"],
         ["7", "예제 config 의 머저 포트가 22", "SSH 포트를 물고 이름에 ADC 종류도 없다"]],
        widths=[0.5, 5.2, CW - 5.7], rh=0.44, size=13.5)
d.note(s, M, 5.65, CW, 0.95, "회귀시키지 말 것",
       "일곱 가지 모두 지금 판에서 고쳐져 있다. 되돌리면 같은 증상이 그대로 돌아온다.", "warn")

s = d.head("개선 · 실운용", "무인 운용에 들어가고 나서야 드러난 것")
d.text(s, M, 2.08, CW, 0.4,
       [[("이 셋은 사람이 화면을 보고 있을 때는 보이지 않는다. 24시간 로테이션을 "
          "돌리기 시작하자 매번 재현됐다.", {"size": 15.5, "color": INK2})]])
for i, (t, body, kind) in enumerate([
        ("결함 1 · 로테이션마다 런 기록이 유실됐다",
         "정지 요청이 오는 순간 상태 대기가 곧바로 실패로 떨어졌다. 데이터 파일은 멀쩡한데 "
         "카탈로그 행이 비고 종료코드가 2 로 나갔다. 감시자는 매 런을 정지 신호로 끝내므로 "
         "이 경로가 매번 재현된다.", "crit"),
        ("결함 2 · 남은 DAQ 가 있으면 옛 런의 TCB 에 붙었다",
         "이전 런의 프로세스가 살아 있으면 새로 띄운 것이 포트를 못 잡고 죽는데, "
         "런컨트롤은 그 포트에 그냥 접속해 옛 런을 붙들고 헛기다렸다. 런 번호도 하나 버렸다.", "warn"),
        ("결함 3 · 실패한 런이 이유를 남기지 않았다",
         "카탈로그에 아무 표기 없는 고아 행만 남았다. 지금은 부팅 실패인지 "
         "시작은 했으나 마감을 못 한 것인지 문장으로 남는다.", "warn")]):
    d.note(s, M, 2.62 + i * 1.32, CW, 1.20, t, body, kind)
d.foot(s, "가짜 검출기로 5종 시험 + 수정 직전 커밋과 A/B 하여 현장 증상을 그대로 재현시켜 진단을 확정했다")

s = d.head("개선 · 데이터", "옮기는 방식을 바꿨다")
d.table(s, M, 2.12, CW, ["", "전", "후"],
        [["파일 이동", "mv / rsync --remove-source-files", "복사 → 체크섬 대조 → 통과한 것만 삭제"],
         ["백업 검증", "원격 파일 개수만 확인", "rsync -c 로 내용 대조. 불일치면 완료 표시 안 함"],
         ["산출물 위치", "로컬에 만들고 심볼릭 링크", "수집처에서 그대로 만든다 — 링크가 낄 자리가 없다"],
         ["모니터링 입력", "분석 쪽 페어링 산출물을 읽음", "PRD 하나만 읽는다 — 남의 진도에 매이지 않는다"]],
        widths=[2.3, 4.4, CW - 6.7], rh=0.52, size=13.5)
d.note(s, M, 4.70, CW, 1.05, "왜 개수로는 부족한가",
       "옮기는 것이 되돌릴 수 없는 데이터이고, 보관 링크가 100 Mb 라 전송이 길고 끊기기 쉽다. "
       "개수만 맞추면 예전에 끊긴 전송의 잔재를 성공으로 오인한다.", "warn")
d.note(s, M, 5.90, CW, 0.9, "덤으로 알게 된 것",
       "체크섬 대조는 전송의 1/18 밖에 들지 않는다. '너무 비싸다' 던 처음 판단이 틀렸다.", "ok")

s = d.head("개선 · 결손", "쓰기 도중 죽은 런이 파이프라인을 영원히 막았다")
d.text(s, M, 2.08, CW, 0.42,
       [[("런이 쓰다 죽으면 마지막 파일이 닫히지 않는다. ROOT 가 열지 못하니 그 런은 "
          "'PRD 개수 = 원시 개수' 를 영원히 만족할 수 없고, 자동 이동 대상이 되지 못한다.",
          {"size": 15.5, "color": INK2})]])
d.code(s, M, 2.52, CW, 1.62, [
    "/Data_ssd/RAW/004293/",
    ("   FADC_004293.root.00000 ~ .00090    91 개    정상", OK),
    ("   Merged/ 91   PRD/ 91               부분 Merged 에서 살린 것 포함", OK),
    ("   badrun/                            격리 사유는 README.txt", WARN),
    ("      FADC_004293.root.00091   5.1 MB  (정상 73 MB)", CRIT),
    ("      SADC_004293.root.00091  0.67 MB  (키가 하나도 없다)", CRIT),
], size=12.5)
d.table(s, M, 4.24, CW, ["판정", "무엇이", "어떻게 한다"],
        [[("bad_raw", {"font": MONO, "bold": True, "color": CRIT}),
          "자기 FADC 또는 SADC 가 안 열린다", "격리한다. 짝은 언제나 함께 옮긴다"],
         [("blocked", {"font": MONO, "bold": True, "color": WARN}),
          "원본은 멀쩡한데 다음 SADC 가 죽었다", "격리하지 않는다. 부분 Merged 에서 PRD 를 살린다"],
         [("gap", {"font": MONO, "bold": True, "color": OK}),
          "전부 잘 열린다", "격리하지 않는다. 재처리하면 되는 것이다"]],
        widths=[1.5, 4.3, CW - 5.8], rh=0.46, size=13.5)
d.note(s, M, 6.14, CW, 1.30, "이동·백업 코드는 한 줄도 고치지 않았다",
       "격리하는 순간 완결 조건이 저절로 풀리고 run 4293 은 보관까지 완주했다. "
       "전 구간 1,972개 런에서 문제 런 631개를 찾아 하나의 목록으로 모았다.", "ok")

s = d.head("개선 · 보안", "DAQ 제어 포트가 인터넷에 열려 있었다")
d.bullets(s, M, 2.12, CW, [
    ("run 4293 이 외부 접속 직후 죽었다. ", "직전까지 완전히 정상이었다(서브런 90, 1,007 Hz)."),
    ("전수 조사 — 6.5개월간 749건, 고유 IP 153개", "가 세 개의 DAQ 포트를 두드리고 있었다."),
    ("유효한 명령값이 맞은 적은 한 번도 없다. ", "피해는 명령이 아니라 대기열 자리를 뺏는 데서 온다 — 대기열이 3뿐이라 스캐너가 자리를 차지하면 런컨트롤의 접속이 밀려난다."),
], size=15.5)
d.code(s, M, 3.95, CW, 1.35, [
    ("변경 전  public 존 : 7809 7813 7814 7815 7816 4280 + NFS", CRIT),
    ("변경 후  여섯 개를 닫았다 — 쓰는 것 셋 + 아무도 듣지 않는 죽은 규칙 셋", OK),
    "",
    "세 노드가 전부 localhost 라 수집에는 영향이 없다. 닫은 직후 1,001 Hz 로 그대로 돎을 확인.",
])
d.note(s, M, 5.55, CW, 1.0, "아직 남은 것",
       "NFS 포트(2049 · 111)는 그대로 열려 있다. 누가 마운트 중인지 확인한 뒤 닫아야 한다. "
       "근본적으로는 DAQ 가 127.0.0.1 에만 bind 하는 것이 옳다 — CUPDAQ 쪽 변경이다.", "crit")

s = d.head("개선 · 장애", "FADC 보드가 걸려 2시간 9분 멎었다")
d.code(s, M, 2.12, CW, 1.95, [
    "03:16:08   run 4294 가 24시간을 채우고 정상 마감",
    ("03:16:21   로테이션 → run 4295 ... 12초 만에 실패", CRIT),
    ("           4296 · 4297 · 4298 · 4299 도 똑같은 자리에서", CRIT),
    ("03:20:17   [SUP] FATAL too many consecutive failures; giving up", CRIT),
    "",
    ("           그리고 두 시간 넘게 아무도 몰랐다", WARN),
])
d.bullets(s, M, 4.28, CW, [
    ("FADC 한 장만 먹통이었다. ", "SADC 는 다섯 런 모두 오류 0건, TCB 도 pedestal 측정까지 정상. 커널에 보드 이탈 기록이 없다 — 빠진 것이 아니라 걸린 것이다."),
    ("네트워크와 무관했다. ", "그 시각 백업 서버 접속도 저장소 마운트도 정상이었다."),
], size=15)
d.note(s, M, 5.70, CW, 1.05, "그래서 만든 것",
       "사람이 손으로 밟은 절차(진단 → usbreset → 짧은 확인 런)를 코드로 옮기고, "
       "알람과 메일을 붙였다. 같은 일이 다시 나면 기계가 먼저 알아채고 스스로 시도한다.", "ok")

# ============================================================ 03 실측
d.section("03", "실측", "숫자로 확인한 것만 적는다")

s = d.head("실측", "성능 — 병목은 CPU 가 아니라 링크였다")
items = [("후처리 · NFS 산출", 41.0, 41.0, "41.0 초/서브런"),
         ("후처리 · 로컬 NVMe", 27.7, 41.0, "27.7 초/서브런   (-32%)"),
         ("모니터링 · /scratch", 14.58, 14.58, "14.58 초/서브런"),
         ("모니터링 · /Data_ssd", 1.11, 14.58, "1.11 초/서브런   (13배)")]
y = 2.20
for name, v, vmax, lab in items:
    d.text(s, M, y, 3.1, 0.28, [[(name, {"size": 14, "color": INK})]])
    d.bar(s, M + 3.2, y + 0.02, 4.6, 0.24, v / vmax,
          color=ACCENT if v == vmax else OK, label=lab)
    y += 0.52
d.text(s, M, 4.40, CW, 0.35,
       [[("링크 실측", {"font": MONO, "size": 12, "bold": True, "color": ACCENT})]])
d.table(s, M, 4.72, CW, ["경로", "속도", "쓰임"],
        [["enp1s0 → 저장소 NFS", ("7.7 MB/s", {"font": MONO, "color": CRIT}), "100 Mb 인터페이스. 이론치의 62%"],
         ["enp0s31f6 → 경희대", ("15.7 MB/s", {"font": MONO, "color": OK}), "1 Gb. 백업은 이쪽이라 서로 굶지 않는다"]],
        widths=[4.0, 2.2, CW - 6.2], rh=0.42, size=13.5)
d.note(s, M, 6.10, CW, 0.75, "이것은 후처리만의 문제가 아니다",
       "원시 데이터도 이 링크로 나간다. 지금은 여유가 있지만 후처리·분석이 겹치면 경합한다.", "warn")

s = d.head("실측", "무엇을 어떻게 확인했는가")
d.table(s, M, 2.12, CW, ["항목", "결과"],
        [["24시간 로테이션", ("4회 통과 — ENDED + exit 0 + 카탈로그 완전 마감", {"color": OK})],
         ["후처리 완결성", ("FADC = SADC = Merged = PRD, 좀비 0건", {"color": OK})],
         ["이동 체인 전 구간", ("run 4292 로 통과. 3단계 348 GB / 10시간 18분", {"color": OK})],
         ["모니터링 대조", ("single 비트 단위 일치, 후보 수 여덟 개 전부 일치", {"color": OK})],
         ["알람·복구 로직", ("가짜 런컨트롤로 10종 — 하드웨어 무접촉", {"color": OK})],
         ["메일 발송", ("책임자 1명 · 전문가 6명 수신 확인", {"color": OK})],
         ["실제 보드 복구", ("다음 장애 때 확인된다", {"color": WARN})]],
        widths=[4.2, CW - 4.2], rh=0.46, size=14)
d.note(s, M, 5.85, CW, 0.95, "검증하지 않은 것을 검증했다고 쓰지 않는다",
       "저장소 문서는 ✅ 와 ⚠️ 를 구분해 적는다. 마지막 줄이 그 예다.", "info")

s = d.head("실측 · 진단", "옛 런의 결손은 데이터 문제가 아니었다")
d.text(s, M, 2.08, CW, 0.42,
       [[("PRD 가 비는 옛 런들을 데이터 손상으로 보고 있었다. 실제로는 보관 서버의 "
          "디렉터리 항목이 깨져 특정 이름의 로그 파일만 만들 수 없는 것이었다.",
          {"size": 15.5, "color": INK2})]])
d.code(s, M, 2.58, CW, 1.72, [
    "date > /scratch/LOG/log_merge_..._run4238_subrun5916.txt",
    ("   -> Input/output error", CRIT),
    ("   이웃 이름(5915 · 5917)은 전부 정상 생성된다. 여유 20T · inode 사용 1%", MUTED),
    "",
    ("껍데기 스크립트가 로그를 먼저 만든다. 그것이 실패하면", WARN),
    ("매크로는 아예 실행되지 않는다 — 후처리는 '(0초) FAILED' 한 줄만 남긴다", WARN),
], size=12.5)
d.bullets(s, M, 4.44, CW, [
    ("성공과 실패가 로그 생성 가능 여부와 정확히 일치했다. ",
     "원시 파일은 전부 정상 크기였다 — 잃은 데이터가 없다."),
    ("우회는 껍데기를 건너뛰고 매크로를 직접 부르는 것. ",
     "run 4238 · 4239 는 이렇게 해서 원시 = Merged = PRD 로 완결됐다."),
    ("★ 직전 merge 로그가 없으면 재처리하지 말 것. ",
     "이어받기 상태를 잃어 개수는 맞지만 내용이 조용히 부족해진다 — PRD 가 없는 것보다 나쁘다."),
], size=15)
d.note(s, M, 6.15, CW, 0.80, "그래서 규칙이 하나 늘었다",
       "0초 만에 실패하면 데이터가 아니라 로그 경로를 먼저 의심한다. 그 이름으로 파일을 "
       "만들 수 있는지 시험해 보면 바로 갈린다.", "info")

# ============================================================ 04 앞으로
d.section("04", "앞으로", "무엇이 남았고, 무엇이 급한가")

s = d.head("앞으로", "우선순위")
d.table(s, M, 2.12, CW, ["", "항목", "왜"],
        [[("사이트", {"color": CRIT, "bold": True, "font": MONO, "size": 11.5}),
          "NFS 포트가 인터넷에 열려 있다", "DAQ 포트보다 성격이 나쁘다. 마운트 중인 쪽을 확인한 뒤 닫는다"],
         [("사이트", {"color": CRIT, "bold": True, "font": MONO, "size": 11.5}),
          "100 Mb 저장소 링크", "고치면 보관 이동이 12시간 → 1~2시간. 수집·후처리·분석이 이 링크를 나눠 쓴다"],
         [("사이트", {"color": WARN, "bold": True, "font": MONO, "size": 11.5}),
          "보관 서버의 깨진 디렉터리 항목", "특정 이름의 로그를 만들 수 없어 후처리가 조용히 빈다. 서버 쪽 점검이 필요하다"],
         [("운용", {"color": WARN, "bold": True, "font": MONO, "size": 11.5}),
          "격리가 남은 옛 런 14개", "원본이 죽은 런이다. 사유를 보고 사람이 판단해야 한다 — 한 런은 빠진 서브런이 156개다"],
         [("운용", {"color": MUTED, "bold": True, "font": MONO, "size": 11.5}),
          "백업이 밀린 옛 런 237개", "저우선순위 백업이 더 급한 작업에 계속 양보하느라 진도가 나가지 않는다"],
         [("코드", {"color": WARN, "bold": True, "font": MONO, "size": 11.5}),
          "같은 오류 반복 시 더 일찍 멈추기", "하드웨어 고장에도 5번까지 새 런 번호를 태운다"],
         [("코드", {"color": MUTED, "bold": True, "font": MONO, "size": 11.5}),
          "libsqlite3 링크 · 단위 테스트 · 소켓 재연결", "지금은 CLI 서브프로세스. 파싱·판정은 순수 함수라 시험하기 쉽다"]],
        widths=[1.1, 4.6, CW - 5.7], rh=0.55, size=13.5)
d.foot(s, "급한 것은 사이트 조치 셋이다. 나머지는 지금 운용을 막지 않는다 — 링크를 고치는 것이 가장 크게 바뀌는 한 가지다")

# ============================================================ 맺음
s = d.head("맺음", "한 줄로 요약하면")
d.box(s, M, 2.30, CW, 1.55, fill=INK)
d.text(s, M + 0.5, 2.68, CW - 1.0, 0.9,
       [[("사람이 지켜봐야 돌던 것을,\n사람이 없어도 돌고 문제가 생기면 사람을 부르는 것으로 바꿨다.",
          {"size": 24, "bold": True, "color": PAPER})]], line_spacing=1.25)
d.bullets(s, M, 4.20, CW, [
    ("정본은 저장소다. ", "이 발표는 읽기용 요약이고, 이어받는 데 필요한 것은 전부 저장소 안에 있다."),
    ("검증 상태를 ✅ 와 ⚠️ 로 구분해 적는다. ", "확인하지 않은 것을 확인했다고 쓰지 않는다."),
    ("설계 판단에는 근거가 있다. ", "되돌리기 전에 CLAUDE.md §5 를 먼저 읽을 것."),
], size=15.5)
d.text(s, M, 6.35, CW, 0.4,
       [[("github.com/Sang-Yong/RENE-daq-rcterm", {"font": MONO, "size": 14, "color": ACCENT})]])

OUT = os.path.join(os.path.dirname(os.path.abspath(__file__)),
                   "..", "..", "docs", "RENE-daq-2026-08-overview-ko.pptx")
OUT = os.path.abspath(OUT)
d.save(OUT)
print("저장 : %s  (%d장)" % (OUT, len(d.p.slides._sldIdLst)))
